#include <unistd.h>

/**
* _putchar - writes character c to stdout
* @c: the char to print
*
* Return: 1 on success.
* On error, returns -1, errno is then set.
*/
int _putchar(char c)
{
 return (write(1, &c, 1));
}
